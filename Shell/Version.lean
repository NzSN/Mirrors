/-! Product release identity, independent of protocol and compiler schema versions. -/

namespace Shell.Version

/-- Mirrors product version. Update when preparing a product release. -/
def version : String := "0.0.1"

end Shell.Version
