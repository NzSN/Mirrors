Codec/Consul.lean:206:37: error: `simp` made no progress
Codec/Consul.lean:206:44: error(lean.unknownIdentifier): Unknown constant `Except.bind_ok`
Codec/Consul.lean:223:4: error: Tactic `rfl` failed: The left-hand side
  do
    let id ← Except.ok s.serviceId
    let host ← Except.ok s.host
    let port ← Except.ok s.port
    let fp ← Except.ok s.certFingerprint
    if (host == "") = true then pure none
      else pure (some { serviceId := id, host := host, port := port, certFingerprint := fp })
is not definitionally equal to the right-hand side
  Except.ok none

case pos
s : ServiceInfo
hkeys :
  List.Pairwise (fun a b => a ≠ b)
    (List.map Prod.fst
      [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
        ("Meta", jMeta s.certFingerprint)])
m0 :
  ("ID", Json.str s.serviceId) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m1 :
  ("Address", Json.str s.host) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m2 :
  ("Port", Json.num (JsonNumber.fromNat s.port)) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m3 :
  ("Meta", jMeta s.certFingerprint) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
h : s.host = ""
hb : (s.host == "") = true
⊢ (do
      let id ← Except.ok s.serviceId
      let host ← Except.ok s.host
      let port ← Except.ok s.port
      let fp ← Except.ok s.certFingerprint
      if (host == "") = true then pure none
        else pure (some { serviceId := id, host := host, port := port, certFingerprint := fp })) =
    Except.ok none
Codec/Consul.lean:226:4: error: Tactic `rfl` failed: The left-hand side
  do
    let id ← Except.ok s.serviceId
    let host ← Except.ok s.host
    let port ← Except.ok s.port
    let fp ← Except.ok s.certFingerprint
    if (host == "") = true then pure none
      else pure (some { serviceId := id, host := host, port := port, certFingerprint := fp })
is not definitionally equal to the right-hand side
  Except.ok (if false = true then none else some s)

case neg
s : ServiceInfo
hkeys :
  List.Pairwise (fun a b => a ≠ b)
    (List.map Prod.fst
      [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
        ("Meta", jMeta s.certFingerprint)])
m0 :
  ("ID", Json.str s.serviceId) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m1 :
  ("Address", Json.str s.host) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m2 :
  ("Port", Json.num (JsonNumber.fromNat s.port)) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
m3 :
  ("Meta", jMeta s.certFingerprint) ∈
    [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
      ("Meta", jMeta s.certFingerprint)]
h : ¬s.host = ""
hb : (s.host == "") = false
⊢ (do
      let id ← Except.ok s.serviceId
      let host ← Except.ok s.host
      let port ← Except.ok s.port
      let fp ← Except.ok s.certFingerprint
      if (host == "") = true then pure none
        else pure (some { serviceId := id, host := host, port := port, certFingerprint := fp })) =
    Except.ok (if false = true then none else some s)
Codec/Consul.lean:225:19: warning: This simp argument is unused:
  if_neg

Hint: Omit it from the simp argument list.
  [apply] simp only [hb]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
Codec/Consul.lean:234:40: error: unsolved goals
s : ServiceInfo
h : s.host ≠ ""
hkeys : List.Pairwise (fun a b => a ≠ b) (List.map Prod.fst [("Service", encService s)])
hm : ("Service", encService s) ∈ [("Service", encService s)]
⊢ ¬Json.mkObj
        [("ID", Json.str s.serviceId), ("Address", Json.str s.host), ("Port", Json.num (JsonNumber.fromNat s.port)),
          ("Meta", jMeta s.certFingerprint)] =
      Json.null
Codec/Consul.lean:238:36: error: Application type mismatch: The argument
  hb
has type
  True
but is expected to have type
  decide ?m.61 = true
in the application
  of_decide_eq_true hb
Codec/Consul.lean:247:8: error: unsolved goals
case nil
h : ∀ (s : ServiceInfo), s ∈ [] → s.host ≠ ""
⊢ (match pure [] with
    | Except.ok ls => Except.ok ls.flatten
    | Except.error e => Except.error e) =
    Except.ok []
Codec/Consul.lean:249:67: error(lean.unknownIdentifier): Unknown constant `Array.toList_toArray`
Codec/Consul.lean:248:17: error: unsolved goals
case cons
s : ServiceInfo
ss : List ServiceInfo
ih : (∀ (s : ServiceInfo), s ∈ ss → s.host ≠ "") → decodeServices (encodeServices ss) = Except.ok ss
h : ∀ (s_1 : ServiceInfo), s_1 ∈ s :: ss → s_1.host ≠ ""
⊢ (match do
      let __do_lift ← decEntry (encEntry s)
      List.cons __do_lift <$> List.mapM (decEntry ∘ encEntry) ss with
    | Except.ok ls => Except.ok ls.flatten
    | Except.error e => Except.error e) =
    Except.ok (s :: ss)
Codec/Consul.lean:249:49: warning: This simp argument is unused:
  List.cons_append

Hint: Omit it from the simp argument list.
  [apply] simp only [decodeServices, encodeServices, Array.toList_toArray, List.mapM_cons,
    decEntry_encEntry s (h s (by simp)), List.flatten]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
Codec/Consul.lean:249:89: warning: This simp argument is unused:
  List.mapM_cons

Hint: Omit it from the simp argument list.
  [apply] simp only [decodeServices, encodeServices, List.cons_append, Array.toList_toArray,
    decEntry_encEntry s (h s (by simp)), List.flatten]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
Codec/Consul.lean:249:105: warning: This simp argument is unused:
  decEntry_encEntry s (h s (by simp))

Hint: Omit it from the simp argument list.
  [apply] simp only [decodeServices, encodeServices, List.cons_append, Array.toList_toArray, List.mapM_cons,
    List.flatten]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
Codec/Consul.lean:249:142: warning: This simp argument is unused:
  List.flatten

Hint: Omit it from the simp argument list.
  [apply] simp only [decodeServices, encodeServices, List.cons_append, Array.toList_toArray, List.mapM_cons,
    decEntry_encEntry s (h s (by simp))]

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
Codec/Consul.lean:250:12: warning: This simp argument is unused:
  ih (fun x hx => h x (by simp [hx]))

Hint: Omit it from the simp argument list.
  [apply] simp

Note: This linter can be disabled with `set_option linter.unusedSimpArgs false`
